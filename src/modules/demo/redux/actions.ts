import { TAction, TActionRequest } from "../../base/types/redux";
import {
  // GetSliderContentRequest,
  // ApiStateType,
} from "../utils/types";
import { ACTION_TYPES } from "./actionTypes";

// export const getSliderContent = (params: GetSliderContentRequest): TActionRequest<GetSliderContentRequest> => {
//   return {
//     type: ACTION_TYPES.GET_SLIDER_CONTENT,
//     params,
//   };
// };

// export const setSliderContentState = (response: ApiStateType): TAction => {
//   return {
//     type: ACTION_TYPES.SET_SLIDER_CONTENT_STATE,
//     response,
//   };
// };